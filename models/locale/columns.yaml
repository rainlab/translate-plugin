# ===================================
#  Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: yes

    code:
        label: Code
        searchable: yes

    is_default:
        label: Default
        type: switch

    is_enabled:
        label: Enabled
        type: switch
        invisible: true
